# Purch

This is the just a rewrite of the python version of Purch in golang. Should just be able to replace the python backend with this and not have any changes to the UI.

(more updates/readme steps to come)
